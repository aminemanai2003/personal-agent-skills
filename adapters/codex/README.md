# Codex Adapter

Codex loads repository-local instructions from `AGENTS.md` and discovers skills from `.agents/skills`. Keep the canonical skill content in `skills/`; package or mirror selected skills into `.agents/skills` during installation.

